appId: com.relay.queue
productName: Relay
asar: true
generateUpdatesFilesForAllChannels: true
files:
  - src/**/*
  - public/**/*
  - package.json
mac:
  category: public.app-category.developer-tools
  target:
    - dmg
    - zip
win:
  target:
    - nsis
    - portable
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
artifactName: Relay-${version}-${os}-${arch}.${ext}
publish:
  provider: github
  owner: patrikkelemen
  repo: relay
  releaseType: release
