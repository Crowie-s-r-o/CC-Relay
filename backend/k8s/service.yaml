apiVersion: v1
kind: Service
metadata:
  name: vibeide
  namespace: vibeide-dev
  labels: {app: vibeide}
spec:
  type: ClusterIP
  selector: {app: vibeide}
  ports: [{name: https, port: 443, targetPort: 8443}]
