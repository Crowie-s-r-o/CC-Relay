{
  "name": "dual-agent-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/electron-main.mjs",
  "description": "Relay, a local sequential task queue for connected subscription-authenticated Codex and Claude Code sessions.",
  "author": "Patrik Kelemen",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "desktop": "electron .",
    "desktop:build:mac": "electron-builder --mac",
    "desktop:build:win": "electron-builder --win",
    "test": "node --test",
    "plugin:install": "node scripts/install-plugin.mjs"
  },
  "dependencies": {
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "electron": "^43.1.1",
    "electron-builder": "^26.15.3"
  }
}
